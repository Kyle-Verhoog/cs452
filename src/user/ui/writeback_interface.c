#include <writeback_interface.h>

void WritebackInterface(){
	// char c;

	// tid_t writer = WhoIs(WRITERSERVICE_UART2_ID);

	// RSProtocol rsp;
	// rsp.rr_req = RR_CHAR;
	// //Register to ReaderService as read char
	// ReaderServiceUART2Register(&rsp);

	// while(true){
	// 	ReadChar(&c);
	// 	WriteChar(writer, c);
	// }

	// Exit();
}
