#include<train_msg.h>

void TrainMsg(){
	// char command[COMMAND_SIZE];
	// int size;

	// tid_t writer = WhoIs(WRITERSERVICE_UART2_ID);

	// RSProtocol rsp;
	// rsp.rr_req = RR_COMMAND;

	// Cursor c;
	// c.row = 1;
	// c.col = 20;

	// //Register to ReaderService as read command
	// ReaderServiceUART2Register(&rsp);

	// while(true){
	// 	//Read size of string, then the string itself
	// 	ReadCommand(command, &size);
	// 	//Append c string terminator character
	// 	WriteCommand(writer, command, size, &c);
	// }

	// Exit();
}

